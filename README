# boulder-arduino

Compile:

arduino-cli compile --fqbn arduino:avr:uno

Install (Linux):

arduino-cli upload -p /dev/ttyACM0 --fqbn arduino:avr:uno

Install (Windows):

arduino-cli upload -p COM5 --fqbn arduino:avr:uno

Connect (Linux):

screen /dev/ttyACM0 115200

Connect (Windows):

putty.exe -> Connection type: Serial -> Serial line: COM5 -> Speed: 115200 - > Button: Open

